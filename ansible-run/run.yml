---
- name: Run Jenkins setup
  hosts: all
  become: true
  tasks:
    - name: List directories
      command: ls -R /path/to/your/ansible-run/roles/
    - include_role:
        name: "{{ role_name }}"



  # 225  sudo apt-add-repository ppa:ansible/ansible
  # 226  sudo apt update -y
  # 227  sudo apt install ansible
  # 228  ansible --version

